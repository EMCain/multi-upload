'use strict';

// constructor
  // has a list of fileListing objects
  // takes the CSS selector of the div you wish to initialize as a parameter
  // creates the needed DOM elements in that div

// upload a batch of files
  // iterate over them and call the appropriate uploading functionality

// upload a single image file

// upload a single non-image file

// construct the generic DOM elements template for any fileListing
